# Python dependencies for the API labs.   pip install -r requirements.txt
# (The generated protobuf/gRPC stubs in Protobuf/labs/python and gRPC/labs/python were produced with
#  grpcio-tools 1.84 and require at least the runtime versions below. Regenerate with the generate.sh
#  scripts if you prefer to use older packages.)

# REST + GraphQL + tests
fastapi>=0.110
httpx>=0.27
uvicorn>=0.29
strawberry-graphql[fastapi]>=0.230

# WebSockets
websockets>=14

# Protobuf + gRPC
protobuf>=7.35.1
grpcio>=1.84.0
grpcio-tools>=1.84.0
grpcio-health-checking>=1.84.0
grpcio-reflection>=1.84.0

# SOAP
zeep>=4.2
lxml>=5

# Webhooks: standard library only
