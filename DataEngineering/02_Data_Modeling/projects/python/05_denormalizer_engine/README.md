# Denormalizer Engine

This project demonstrates how to take a set of normalized dimension and fact tables (simulated as Pandas DataFrames) and join them into a single wide, denormalized table suitable for analytics and machine learning.

## Dependencies
- `pandas`

## How to Run
```bash
pip install pandas
python main.py
```
