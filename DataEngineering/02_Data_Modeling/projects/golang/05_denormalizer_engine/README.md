# 05 Denormalizer Engine

This project demonstrates a simple denormalizer engine using Go structs. 
It takes mock structured relational data (Customers and Orders) and flattens it into a single wide struct/map representation.

## Running the project

1. Initialize a Go module (if not done): `go mod init denormalizer`
2. Run the code: `go run main.go`
