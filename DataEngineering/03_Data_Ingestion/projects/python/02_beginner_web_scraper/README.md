# Beginner Web Scraper

A web scraper using `requests` and `BeautifulSoup` that extracts quotes from `quotes.toscrape.com` and stores them as JSON.

## Usage

1. Install requirements: `pip install requests beautifulsoup4`
2. Run the script: `python main.py`
