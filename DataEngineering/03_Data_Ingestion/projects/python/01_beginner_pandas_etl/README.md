# Beginner Pandas ETL

A simple ETL script using Pandas to read a sample CSV, clean the data (handle nulls, deduplicate), and write it to a local SQLite database.

## Usage

1. Install requirements: `pip install pandas`
2. Run the script: `python main.py`
