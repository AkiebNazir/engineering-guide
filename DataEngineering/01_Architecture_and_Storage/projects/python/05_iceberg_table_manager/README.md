# Iceberg Table Manager

This script provides a mock implementation of Apache Iceberg table management. It simulates defining a schema, setting a partition spec, and managing metadata versions as data is appended to the table.

*(Note: Real implementations would use `pyiceberg` connected to a Rest Catalog or Hive Metastore.)*

## Usage

Run the script:

```bash
python main.py
```
