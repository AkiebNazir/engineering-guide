# Parquet vs CSV Benchmarker

This project uses `pandas` and `pyarrow` to generate a large dataset and compare the performance and storage footprint of CSV and Parquet file formats.

## Prerequisites

Install the required packages:

```bash
pip install pandas pyarrow numpy
```

## Usage

Run the script to see the benchmarks:

```bash
python main.py
```
