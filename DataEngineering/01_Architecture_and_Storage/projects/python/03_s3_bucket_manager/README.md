# S3 Bucket Manager

This project uses `boto3` and `moto` to simulate interacting with an AWS S3 bucket. It demonstrates creating a bucket, uploading a file, listing objects, and reading object contents.

## Prerequisites

Install the required packages:

```bash
pip install boto3 moto
```

## Usage

Run the script:

```bash
python main.py
```
