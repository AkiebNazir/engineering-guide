# 01_local_data_lake_simulation

This project simulates a local data lake structure.

## How to Run

1. Run the script:
   ```bash
   go run main.go
   ```
2. Verify the output:
   Check the created directory structure `data_lake/raw/year=2023/month=10/` to find the generated `data.json`.
