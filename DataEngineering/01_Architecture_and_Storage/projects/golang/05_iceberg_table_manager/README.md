# 05_iceberg_table_manager

This script simulates managing Apache Iceberg table metadata by tracking table snapshots locally using JSON.

## How to Run

1. Run the script:
   ```bash
   go run main.go
   ```
2. Verify output:
   The script will generate a `v1.metadata.json` file representing a mocked Iceberg metadata tracking file.
