/* Live request flows (see sd-flow.js for the engine and data format). */
'use strict';
{

/* ======================================================== ReAct agent loop == */
const Lagent = lane([
  { label: 'USER', nodes: [{ id: 'user', label: 'User', sub: 'asks in plain text', kind: 'client', row: 1 }] },
  { label: 'HARNESS', nodes: [
    { id: 'harness', label: 'Agent harness', sub: 'loop · parse · run', icon: 'agent', row: 1 },
    { id: 'pad', label: 'Scratchpad', sub: 'append-only transcript', icon: 'doc', row: 2 }] },
  { label: 'MODEL', nodes: [{ id: 'llm', label: 'LLM', sub: 'stateless call', icon: 'llm', row: 0 }] },
  { label: 'TOOLS', nodes: [
    { id: 'search', label: 'search', sub: 'web lookup', icon: 'search', row: 3 },
    { id: 'weather', label: 'get_weather', sub: 'returns °C', icon: 'tool', row: 1 },
    { id: 'calc', label: 'calculator', sub: 'exact math', icon: 'number', row: 2 }] },
]);
defineFlow('flow-agent-loop', {
  title: 'Live flow: one agent run, call by call',
  hint: 'Follow the weather question through three LLM calls and two tool calls, then watch the harness survive a malformed reply and a stuck loop.',
  zones: Lagent.zones, h: Lagent.h, nodes: Lagent.nodes,
  edges: [
    ['user', 'harness'], ['harness', 'llm'], ['harness', 'pad'],
    ['harness', 'search'], ['harness', 'weather'], ['harness', 'calc'],
  ],
  scenarios: [
    {
      id: 'weather', name: 'Weather in °F',
      summary: '"What\'s the weather in Paris right now, in Fahrenheit?" <b>Three LLM calls, two tool calls, one answer.</b> Timings are rough, illustrative values.',
      steps: [
        { path: ['user', 'harness'], label: 'weather in Paris, in °F?', ms: 20, title: 'The question reaches the harness',
          detail: 'The harness is ordinary code: a loop, a parser and a tool dispatcher. The model can\'t know today\'s weather and is unreliable at exact arithmetic, so it will need tools.' },
        { path: ['harness', 'llm', 'harness'], label: 'call 1: system + tools + question', badgeAt: 'llm', badge: 'Action: get_weather', ms: 800,
          title: 'Call 1: the model asks for a tool',
          detail: 'The model only <b>writes text</b>: <code>Action: get_weather</code>, <code>Action Input: {"city": "Paris"}</code>. It never runs anything itself; the harness parses that text into a tool name and arguments.' },
        { path: ['harness', 'weather', 'harness'], label: 'get_weather("Paris")', badgeAt: 'weather', badge: '18°C', ms: 200,
          title: 'The harness runs the tool',
          detail: 'This is where the side effect happens, in your code, not in the model. <code>18°C</code> is an example value.' },
        { path: ['harness', 'pad'], label: 'Observation: 18°C', ms: 1, title: 'The result is appended to the transcript',
          detail: 'The scratchpad is the agent\'s <b>only</b> memory. Delete this line and the agent has forgotten the weather; there is nowhere else it could be stored.' },
        { path: ['harness', 'llm', 'harness'], label: 'call 2: everything so far', badgeAt: 'llm', badge: 'Action: calculator', ms: 900,
          title: 'Call 2 re-sends the whole transcript',
          detail: 'Each call is stateless, so call <i>k</i> re-sends everything before it. That is why long runs cost O(k²) tokens: at 500 + 300 tokens/step, 20 steps process about 67,000 tokens in total.' },
        { path: ['harness', 'calc', 'harness'], label: '18 * 9 / 5 + 32', badgeAt: 'calc', badge: '64.4', ms: 5,
          title: 'Exact arithmetic by a tool', detail: 'The calculator gets the conversion exactly right, which the model on its own might not.' },
        { path: ['harness', 'pad'], label: 'Observation: 64.4', ms: 1, title: 'Second observation appended',
          detail: 'The transcript now holds question, two actions and two observations: everything the next call will see.' },
        { path: ['harness', 'llm', 'harness'], label: 'call 3: everything so far', badgeAt: 'llm', badge: 'Final Answer: …', tone: 'ok', ms: 900,
          title: 'Call 3: the model writes a Final Answer',
          detail: 'The harness checks whether the reply starts with <code>Final Answer:</code> before trying to parse an action. That check is the normal way the loop stops.' },
        { path: ['harness', 'user'], label: "It's 18°C (64.4°F)", tone: 'ok', ms: 20, title: 'Answer returned, loop terminates',
          detail: 'Three LLM calls, two tool calls. Every agent framework is a variation of this loop, parser, dispatcher and stop check.' },
      ],
    },
    {
      id: 'parse', name: 'Malformed reply',
      summary: 'The model writes an action the parser can\'t read. A good harness <b>feeds the error back</b> as an observation instead of crashing.',
      steps: [
        { path: ['harness', 'llm', 'harness'], label: 'call k', ms: 800, title: 'A normal call',
          detail: 'Prompt = system prompt + tool schemas + question + <code>scratchpad.render()</code>.' },
        { at: 'harness', badge: 'ParseError: invalid JSON', tone: 'warn', ms: 1, title: 'The action can\'t be parsed',
          detail: 'The reply was <code>Action Input: {"city": "Paris"</code> with the closing brace missing. <code>parse_action</code> raises a typed <code>ParseError</code>, never a silent <code>None</code>.' },
        { path: ['harness', 'pad'], label: 'Observation: PARSE_ERROR …', tone: 'warn', ms: 1, title: 'The error becomes an observation',
          detail: '"PARSE_ERROR: … Reformat as Action/Action Input." goes into the transcript like any tool result. The loop keeps going; it doesn\'t throw.' },
        { path: ['harness', 'llm', 'harness'], label: 'call k+1 (sees the error)', badgeAt: 'llm', badge: 'valid Action', tone: 'ok', ms: 900,
          title: 'The model corrects itself',
          detail: 'The next call contains the parser\'s own error message, so the model can see what was wrong and reformat. This costs one extra step from the <code>max_steps</code> budget.' },
        { path: ['harness', 'weather', 'harness'], label: 'get_weather("Paris")', badgeAt: 'weather', badge: '18°C', tone: 'ok', ms: 200,
          title: 'The run continues as normal',
          detail: 'An unknown tool name is handled the same way: <code>ERROR: unknown tool</code> as an observation, not an uncaught exception.' },
      ],
    },
    {
      id: 'stuck', name: 'Stuck in a loop',
      summary: 'The model keeps retrying a query that can\'t succeed. A <b>repetition detector</b> stops it; the step limit alone is a very expensive safety net.',
      steps: [
        { path: ['harness', 'llm', 'harness'], label: 'call 4', badgeAt: 'llm', badge: 'search("Paris weather 2031")', ms: 800,
          title: 'The model asks for an impossible search', detail: 'Nothing wrong yet: tools can legitimately return nothing.' },
        { path: ['harness', 'search', 'harness'], label: 'search(…)', badgeAt: 'search', badge: 'no results', tone: 'warn', ms: 300,
          title: 'No results', detail: 'The observation "no results" is appended to the scratchpad.' },
        { path: ['harness', 'llm', 'harness'], label: 'call 5', badgeAt: 'llm', badge: 'same action again', tone: 'warn', ms: 900,
          title: 'The same action, verbatim', detail: 'The harness hashes each (tool, arguments) pair. This hash matches the previous step.' },
        { path: ['harness', 'search', 'harness'], label: 'search(…)', badgeAt: 'search', badge: 'no results', tone: 'warn', ms: 300,
          title: 'Same result', detail: 'Repeating an identical call to a deterministic tool will not produce anything new.' },
        { path: ['harness', 'llm', 'harness'], label: 'call 6', badgeAt: 'llm', badge: 'same action again', tone: 'warn', ms: 900,
          title: 'A third identical action', detail: 'Now the same action hash has been seen three times in a row.' },
        { at: 'harness', badge: 'same hash 3× → break', tone: 'err', ms: 1, title: 'The repetition detector stops the run',
          detail: 'Without it, the loop burns every remaining step up to <code>max_steps</code> and then raises <code>max_steps exceeded without a Final Answer</code>, paying for every call on the way.' },
      ],
    },
  ],
});

/* ================================================================== RAG == */
const Lrag = lane([
  { label: 'SOURCE', nodes: [{ id: 'pdf', label: 'Handbook PDF', sub: 'incl. page 14', icon: 'doc', row: 1 }] },
  { label: 'INDEX', nodes: [
    { id: 'bm25', label: 'BM25 index', sub: 'inverted index', icon: 'index', row: 0 },
    { id: 'chunk', label: 'Chunk + embed', sub: 'offline, once', icon: 'layers', row: 1 },
    { id: 'vec', label: 'Vector index', sub: 'dense embeddings', icon: 'vector', row: 2 }] },
  { label: 'QUERY', nodes: [
    { id: 'fuse', label: 'Hybrid search', sub: 'both indexes · RRF', icon: 'search', row: 1 },
    { id: 'user', label: 'Employee', sub: 'asks a question', kind: 'client', row: 3 }] },
  { label: 'ANSWER', nodes: [
    { id: 'rerank', label: 'Re-ranker', sub: 'cross-encoder', icon: 'sort', row: 1 },
    { id: 'pack', label: 'Prompt packer', sub: 'order + budget', icon: 'prompt', row: 2 },
    { id: 'llm', label: 'LLM', sub: 'reads, answers', icon: 'llm', row: 3 }] },
]);
defineFlow('flow-rag', {
  title: 'Live flow: index the handbook, then answer from it',
  hint: 'Run the offline indexing phase, then a question through hybrid retrieval, fusion, re-ranking and the LLM. The last tab shows why keyword search is kept alongside vectors.',
  zones: Lrag.zones, h: Lrag.h, nodes: Lrag.nodes,
  edges: [
    ['pdf', 'chunk'], ['chunk', 'bm25'], ['chunk', 'vec'],
    ['user', 'fuse'], ['fuse', 'bm25'], ['fuse', 'vec'],
    ['fuse', 'rerank'], ['rerank', 'pack'], ['pack', 'llm'], ['llm', 'user'],
  ],
  scenarios: [
    {
      id: 'index', name: 'Indexing (offline)',
      summary: 'Prepare the library <b>once per document set</b>, so each question later needs only a few lookups. Timings are rough, illustrative values.',
      steps: [
        { path: ['pdf', 'chunk'], label: 'handbook.pdf', ms: 50, title: 'The handbook is loaded',
          detail: 'Text is extracted from the PDF. Page 14 says: "Employees in their first year receive 15 days of paid vacation."' },
        { at: 'chunk', badge: 'split into passages', ms: 500, title: 'Chunking',
          detail: 'Fixed-size chunks with some overlap, so "15 days" and "first year" are less likely to be split across a boundary. A fact cut in half is unretrievable by either half.' },
        { path: ['chunk', 'bm25'], label: 'term → (chunk, tf)', ms: 200, title: 'Build the inverted index',
          detail: 'For every term, a posting list of the chunks containing it and how often. Querying later only touches the query\'s own terms, never the whole corpus.' },
        { path: ['chunk', 'vec'], label: 'embed(chunk)', badgeAt: 'vec', badge: 'vectors stored', tone: 'ok', ms: 2000,
          title: 'Embed every chunk',
          detail: 'The expensive encoding is done once, here. At query time only the question needs embedding, which is why a bi-encoder scales to millions of chunks.' },
      ],
    },
    {
      id: 'query', name: 'Query: vacation days',
      summary: '"How many vacation days do new employees get?" <b>Hybrid retrieval, fusion, re-rank, pack, answer.</b> Timings are rough, illustrative values.',
      steps: [
        { path: ['user', 'fuse'], label: 'vacation days for new employees?', ms: 20, title: 'The question arrives',
          detail: 'It is sent to two different retrievers, in parallel, because each one catches what the other misses.' },
        { path: ['fuse', 'bm25', 'fuse'], label: 'vacation days new employees', badgeAt: 'bm25', badge: 'C, A, B', ms: 5,
          title: 'Sparse search: exact words',
          detail: 'BM25 scores chunks by the query terms they contain, with term-frequency saturation and length normalisation. Its scores are unbounded and corpus-dependent.' },
        { path: ['fuse', 'vec', 'fuse'], label: 'embed(query) → top-N', badgeAt: 'vec', badge: 'B, C, D', ms: 20,
          title: 'Dense search: same meaning',
          detail: 'The question is embedded with the same model as the chunks, and the nearest vectors come back. This finds paraphrases BM25 cannot, such as "paid time off".' },
        { at: 'fuse', badge: 'RRF → C, B, A, D', tone: 'ok', ms: 1, title: 'Reciprocal Rank Fusion',
          detail: 'Raw scores can\'t be added, so RRF sums <code>1 / (60 + rank)</code>. C scores 1/61 + 1/62 ≈ 0.0325 and wins: chunks that <b>both</b> retrievers like float to the top.' },
        { path: ['fuse', 'rerank'], label: 'top 100 candidates', ms: 80, title: 'Re-rank a short list',
          detail: 'A cross-encoder reads query and chunk <b>together</b>, one full transformer pass per pair. Far more precise, far too slow for the whole corpus, fine for ~100 candidates.' },
        { path: ['rerank', 'pack'], label: 'best 3–5 (page 14 first)', ms: 1, title: 'Keep only the best few',
          detail: 'If the right page were candidate #40 and only the top 5 were kept, it would be lost here. Re-ranking is what makes a small k safe.' },
        { path: ['pack', 'llm'], label: 'context + question', ms: 600, title: 'Pack the prompt',
          detail: 'The most relevant chunks go at the start and the end (the "sandwich"), because models use the middle of a long context least reliably. The budget stays well below the window.' },
        { path: ['llm', 'user'], label: '15 days in the first year', tone: 'ok', ms: 30, title: 'An answer read off the page',
          detail: 'An open-book exam: the model answers from page 14, not from memory. Without RAG it could only guess "most companies give 10–20 days".' },
      ],
    },
    {
      id: 'rare', name: 'Exact rare token',
      summary: 'Query: <b>"error E-4012"</b>. Dense embeddings blur exact codes; BM25 nails them. This is why hybrid search keeps both.',
      steps: [
        { path: ['user', 'fuse'], label: 'error E-4012', ms: 20, title: 'A query with an exact code',
          detail: 'IDs, error codes and rare technical terms are where the two retrievers disagree most.' },
        { path: ['fuse', 'vec', 'fuse'], label: 'embed("error E-4012")', badgeAt: 'vec', badge: 'other error codes', tone: 'warn', ms: 20,
          title: 'Dense search blurs the code',
          detail: 'To an embedding model one error code looks much like another, so the exact page may rank below similar-looking ones.' },
        { path: ['fuse', 'bm25', 'fuse'], label: 'E-4012', badgeAt: 'bm25', badge: 'exact match #1', tone: 'ok', ms: 5,
          title: 'BM25 finds the exact token',
          detail: 'A rare term has a high IDF, so the one chunk containing <code>E-4012</code> scores far above the rest. The reverse also happens: "car insurance" vs "automobile coverage" scores 0 in BM25.' },
        { path: ['fuse', 'rerank'], label: 'sparse #1 survives fusion', ms: 80, title: 'Fusion keeps it in the candidate list',
          detail: 'Being #1 in just one list still earns 1/61 in RRF, enough to stay among the candidates for the re-ranker.' },
        { path: ['rerank', 'pack', 'llm'], label: 'the E-4012 page', ms: 600, title: 'The re-ranker confirms it',
          detail: 'Reading query and chunk together, the cross-encoder sees the exact code match and puts that page first in the prompt.' },
        { path: ['llm', 'user'], label: 'what E-4012 means', tone: 'ok', ms: 30, title: 'Correct answer',
          detail: 'Neither retriever wins every query; hybrid search with rank fusion covers both failure modes.' },
      ],
    },
  ],
});

/* ======================================================== LLM inference == */
const Linf = lane([
  { label: 'CLIENT', nodes: [{ id: 'client', label: 'Client', sub: 'streamed reply', kind: 'client', row: 1 }] },
  { label: 'SERVER', nodes: [
    { id: 'tok', label: 'Tokenizer', sub: 'text ↔ token IDs', icon: 'text', row: 0 },
    { id: 'sched', label: 'Scheduler', sub: 'continuous batching', icon: 'scheduler', row: 2 }] },
  { label: 'GPU COMPUTE', nodes: [
    { id: 'prefill', label: 'Prefill', sub: 'compute-bound', icon: 'cpu', row: 0 },
    { id: 'sample', label: 'Sampler', sub: '÷T · top-k/p · draw', icon: 'filter', row: 1 },
    { id: 'decode', label: 'Decode step', sub: 'memory-bound', icon: 'memory', row: 2 }] },
  { label: 'GPU MEMORY', nodes: [{ id: 'kv', label: 'KV cache', sub: 'paged, 16-token blocks', icon: 'cache', row: 1 }] },
]);
defineFlow('flow-llm-inference', {
  title: 'Live flow: one prompt through an LLM server',
  hint: 'Watch one compute-bound prefill, then memory-bound decode steps that each read the whole KV cache. The other tabs show shared prefixes and continuous batching.',
  zones: Linf.zones, h: Linf.h, nodes: Linf.nodes,
  edges: [
    ['client', 'tok'], ['tok', 'sched'], ['sched', 'prefill'], ['sched', 'decode'],
    ['prefill', 'kv'], ['decode', 'kv'], ['prefill', 'sample'], ['decode', 'sample'], ['sample', 'tok'],
  ],
  scenarios: [
    {
      id: 'one', name: 'One request',
      summary: 'Prompt <b>"The cat sat on the"</b>. One prefill over the whole prompt, then one decode step per generated token. Timings are rough, illustrative values.',
      steps: [
        { path: ['client', 'tok'], label: '"The cat sat on the"', ms: 20, title: 'The prompt arrives',
          detail: 'The client asks for a streamed response, so each token can be shown as soon as it is picked.' },
        { path: ['tok', 'sched'], label: '~5 token IDs', ms: 1, title: 'Text becomes token IDs',
          detail: 'The model never sees characters, only IDs from its vocabulary. Every cost from here on is counted in tokens.' },
        { path: ['sched', 'prefill'], label: 'admit, k tokens', ms: 1, title: 'The scheduler admits the request',
          detail: 'It checks there are enough free KV-cache blocks. At long context, memory, not compute, limits how many sequences fit at once.' },
        { path: ['prefill', 'kv'], label: 'K, V for all k tokens', badgeAt: 'kv', badge: 'one-time write', ms: 150,
          title: 'Prefill: the whole prompt in parallel',
          detail: 'Q, K, V for every prompt token at every layer, in one big matrix multiply: high arithmetic intensity, so it is <b>compute-bound</b>. Its length largely sets time-to-first-token.' },
        { path: ['prefill', 'sample', 'tok', 'client'], label: '"mat"', badgeAt: 'sample', badge: 'mat', tone: 'ok', ms: 5,
          title: 'First token sampled and streamed',
          detail: 'Logits ÷ temperature, optional top-k / top-p masks, softmax, then a random draw (or argmax if greedy). The tokenizer turns the ID back into text.' },
        { path: ['decode', 'kv'], label: 'read cache + append k,v', ms: 25, title: 'Decode: one new token',
          detail: 'Only the newest token\'s q, k, v are computed; k and v are appended, and q is scored against every cached key. Reading the whole cache for so little math makes decode <b>memory-bound</b>.' },
        { path: ['decode', 'sample', 'tok', 'client'], label: '"."', badgeAt: 'sample', badge: '.', ms: 5,
          title: 'Next token streamed',
          detail: 'Without the KV cache every step would recompute K and V for all earlier tokens: O(n) redundant work per token.' },
        { path: ['decode', 'sample'], label: 'logits', badgeAt: 'sample', badge: '[END]', tone: 'ok', ms: 25,
          title: 'An END token stops generation',
          detail: 'Three separate predictions made the sentence. The sequence\'s KV blocks go back to the free pool for the next request.' },
      ],
    },
    {
      id: 'prefix', name: 'Shared system prompt',
      summary: 'A second request with the <b>same system prompt</b>. Paged blocks can be shared between sequences, so that prefix is stored and computed once.',
      steps: [
        { path: ['client', 'tok', 'sched'], label: 'same system prompt + new Q', ms: 20, title: 'A request with a familiar prefix',
          detail: 'Chat products send the same long system prompt with every request, so most of each prompt has been seen before.' },
        { at: 'sched', badge: 'prefix blocks found', tone: 'ok', ms: 1, title: 'The prefix is already cached',
          detail: 'The new sequence\'s block table points at the same physical blocks as the earlier request; each block\'s reference count goes to 2.' },
        { path: ['sched', 'prefill'], label: 'only the new tokens', ms: 20, title: 'Prefill only what is new',
          detail: 'Only the question\'s tokens need Q, K, V computed, so time-to-first-token drops sharply.' },
        { path: ['prefill', 'kv'], label: 'new blocks only', badgeAt: 'kv', badge: 'shared · ref count 2', tone: 'ok', ms: 5,
          title: 'Memory for the prefix is not duplicated',
          detail: 'For a 7B-class model at fp16, 4,096 tokens of KV cache cost about 2.15 GB. Sharing a long prefix saves that per extra sequence.' },
        { path: ['prefill', 'sample', 'tok', 'client'], label: 'token 1', tone: 'ok', ms: 5, title: 'First token, sooner',
          detail: 'Same sampling as before; only the prefill work was cut.' },
        { path: ['decode', 'kv'], label: 'write to a shared block', badgeAt: 'kv', badge: 'copy-on-write', tone: 'warn', ms: 25,
          title: 'Diverging from the shared prefix',
          detail: 'A shared block is copied only when one sequence needs to write into it, just like copy-on-write pages in an operating system.' },
      ],
    },
    {
      id: 'batch', name: 'Continuous batching',
      summary: 'Sequences A and B are decoding together. When A finishes, <b>C joins at once</b>, without waiting for the whole batch to end.',
      steps: [
        { at: 'decode', badge: 'batch: A + B', ms: 25, title: 'One decode step for the whole batch',
          detail: 'Batching shares each read of the model weights across sequences, which is the main way to raise throughput in a memory-bound phase.' },
        { path: ['decode', 'sample', 'tok', 'client'], label: 'A: [END]', badgeAt: 'sample', badge: 'A done', tone: 'ok', ms: 5,
          title: 'Sequence A finishes', detail: 'Output lengths are unknown in advance and vary widely, so sequences finish at different times.' },
        { at: 'kv', badge: "A's blocks → free pool", ms: 1, title: 'Memory freed block by block',
          detail: 'Paged allocation leaves at most one partly filled block per sequence, where contiguous max-length buffers wasted 60–80% of memory.' },
        { path: ['sched', 'prefill'], label: 'admit C (chunked)', tone: 'ok', ms: 30, title: 'C starts right away',
          detail: 'Its prefill is split into small chunks between decode steps. One long, compute-heavy prefill would otherwise stall B\'s tokens (head-of-line blocking).' },
        { path: ['prefill', 'kv'], label: "C's prompt K, V", ms: 30, title: "C's prompt is written to the cache",
          detail: 'New blocks come from the free pool, and they don\'t need to be next to each other in memory.' },
        { path: ['sched', 'decode'], label: 'B + C step together', ms: 25, title: 'The batch continues with B and C',
          detail: 'The batch is re-formed at every step, so the GPU is never left waiting for its longest sequence.' },
      ],
    },
  ],
});

/* ================================================================= MCP == */
const Lmcp = lane([
  { label: 'USER', nodes: [{ id: 'user', label: 'User', sub: 'asks a question', kind: 'client', row: 1 }] },
  { label: 'AGENT HOST', nodes: [
    { id: 'llm', label: 'External LLM', sub: 'sees no credentials', icon: 'llm', row: 0 },
    { id: 'client', label: 'MCP client', sub: 'inside the agent', icon: 'agent', row: 1 }] },
  { label: 'MCP SERVER', nodes: [{ id: 'server', label: 'MCP server', sub: 'holds DB creds', icon: 'server', row: 1 }] },
  { label: 'ENTERPRISE', nodes: [
    { id: 'db', label: 'Employee DB', sub: 'private data', kind: 'db', row: 1 },
    { id: 'logs', label: 'Log file', sub: 'a resource', icon: 'logs', row: 2 }] },
]);
defineFlow('flow-mcp', {
  title: 'Live flow: an agent uses a tool over MCP',
  hint: 'Discover what a server offers, then follow a tool call through the air-gap: the LLM asks, the client calls, and only the server touches the database.',
  zones: Lmcp.zones, h: Lmcp.h, nodes: Lmcp.nodes,
  edges: [
    ['user', 'client'], ['client', 'llm'], ['client', 'server'], ['server', 'db'], ['server', 'logs'],
  ],
  scenarios: [
    {
      id: 'discover', name: 'Discovery',
      summary: 'The client connects and asks the server what it offers. <b>No custom integration code</b>: both sides speak the same protocol. Timings are illustrative.',
      steps: [
        { path: ['client', 'server', 'client'], label: 'initialize', badgeAt: 'server', badge: 'tools · resources · prompts', ms: 10,
          title: 'Handshake',
          detail: 'MCP messages are JSON-RPC. Client and server agree a protocol version and state their capabilities; the server here offers tools, resources and prompts.' },
        { path: ['client', 'server', 'client'], label: 'tools/list', badgeAt: 'server', badge: 'query_employee_db', ms: 5,
          title: 'What can I call?',
          detail: 'Each tool comes back with a name, a description and a JSON Schema for its arguments. That is exactly what the agent will show the model.' },
        { path: ['client', 'server', 'client'], label: 'resources/list', badgeAt: 'server', badge: 'log file, pages', ms: 5,
          title: 'What can I read?',
          detail: 'Resources are data the agent can read; tools are functions it can execute; prompts are reusable templates the server provides.' },
        { at: 'client', badge: 'ready, zero glue code', tone: 'ok', ms: 1, title: 'N × M becomes N + M',
          detail: 'Without a standard, 10 agent frameworks × 50 data sources is 500 custom integrations. With MCP, each framework needs one client and each source one server.' },
      ],
    },
    {
      id: 'call', name: 'Tool call (air-gap)',
      summary: '"Who manages team X?" The external LLM <b>never sees the database credentials</b>. It can only ask; the MCP server does the work.',
      steps: [
        { path: ['user', 'client'], label: 'Who manages team X?', ms: 20, title: 'The question reaches the agent',
          detail: 'The MCP client lives inside the agent (Claude Desktop, a LangChain script, a custom swarm).' },
        { path: ['client', 'llm'], label: 'question + tool schemas', ms: 800, title: 'The model is shown the tools',
          detail: 'The schemas from <code>tools/list</code> go into the prompt. The model sees names and arguments, not connection strings.' },
        { path: ['llm', 'client'], label: 'call query_employee_db', ms: 1, title: 'The LLM asks for a tool',
          detail: 'It returns a structured request (tool name + arguments). Asking is all it can do; it has no network path to the database.' },
        { path: ['client', 'server'], label: 'tools/call', ms: 5, title: 'The client forwards the call',
          detail: 'A <code>tools/call</code> request with the tool name and arguments, sent to the server that registered that tool.' },
        { path: ['server', 'db', 'server'], label: 'SQL (server\'s creds)', badgeAt: 'db', badge: 'rows', ms: 20,
          title: 'Only the server touches the DB',
          detail: 'Credentials live in the server\'s own configuration, and it runs a query it controls. This is the air-gap: a trust boundary between the model and your data.' },
        { path: ['server', 'client'], label: 'result → context', ms: 5, title: 'The result comes back as content',
          detail: 'The server returns only the rows needed to answer, as text content the agent can put in front of the model.' },
        { path: ['client', 'llm', 'client'], label: 'context → answer', ms: 800, title: 'The model writes the answer',
          detail: 'It reads the returned rows like any other context. It has seen data, never the credentials that fetched it.' },
        { path: ['client', 'user'], label: 'answer', tone: 'ok', ms: 20, title: 'Answer delivered',
          detail: 'Swap the database or rotate its password and nothing changes on the LLM side, only in the server.' },
      ],
    },
    {
      id: 'resource', name: 'Read a resource',
      summary: 'Resources are <b>data to read</b>, not functions to run: a log file, a Notion page. Same protocol, same trust boundary.',
      steps: [
        { path: ['client', 'server'], label: 'resources/read', ms: 5, title: 'The agent asks for a resource',
          detail: 'The client names a resource it saw in <code>resources/list</code>, by its URI.' },
        { path: ['server', 'logs', 'server'], label: 'read file', badgeAt: 'logs', badge: 'contents', ms: 10,
          title: 'The server reads it',
          detail: 'The server decides which files are exposed at all. The agent can\'t browse the filesystem around them.' },
        { path: ['server', 'client'], label: 'resource contents', ms: 5, title: 'Contents returned',
          detail: 'Unlike a tool call, reading a resource has no side effects.' },
        { path: ['client', 'llm', 'client'], label: 'log excerpt + question', ms: 800, title: 'Used as context',
          detail: 'The model sees the text of the file and nothing about where or how it is stored.' },
        { path: ['client', 'user'], label: 'answer', tone: 'ok', ms: 20, title: 'Answer delivered',
          detail: 'Tools, resources and prompts are the three primitives an MCP server can offer.' },
      ],
    },
  ],
});

/* ======================================================= Place order ====== */
const Lord = lane([
  { label: 'DRIVING ADAPTER', nodes: [{ id: 'http', label: 'HTTP adapter', sub: 'parse · call · map', icon: 'api', row: 1.5 }] },
  { label: 'APPLICATION', nodes: [{ id: 'uc', label: 'PlaceOrder', sub: 'use case, owns txns', icon: 'workflow', row: 1.5 }] },
  { label: 'DOMAIN', nodes: [{ id: 'order', label: 'Order', sub: 'rules, no I/O', icon: 'cart', row: 1.5 }] },
  { label: 'PORTS → ADAPTERS', nodes: [
    { id: 'catalog', label: 'PriceCatalog', sub: 'price_of(sku)', icon: 'tag', row: 0 },
    { id: 'uow', label: 'UnitOfWork', sub: 'SQLite · commit', icon: 'sqlite', row: 1 },
    { id: 'pay', label: 'PaymentGateway', sub: 'charge(key)', icon: 'payment', row: 2 },
    { id: 'pub', label: 'EventPublisher', sub: 'publish(event)', icon: 'stream', row: 3 }] },
]);
defineFlow('flow-place-order', {
  title: 'Live flow: "place order" through every layer',
  hint: 'Follow one order from the HTTP adapter through the use case, the domain and each port. The order of the steps is the design: record intent, charge idempotently, record the outcome.',
  zones: Lord.zones, h: Lord.h, nodes: Lord.nodes,
  edges: [
    ['http', 'uc'], ['uc', 'order'],
    ['uc', 'catalog'], ['uc', 'uow'], ['uc', 'pay'], ['uc', 'pub'],
  ],
  scenarios: [
    {
      id: 'paid', name: 'Paid',
      summary: 'Customer c1 orders 3 apples at 120¢. The use case <b>orchestrates</b>; rules live in <code>Order</code>; every I/O goes through a port. Timings are illustrative.',
      steps: [
        { path: ['http', 'uc'], label: 'PlaceOrderCommand', ms: 2, title: 'The adapter parses and calls',
          detail: 'Syntactic validation happens at the edge: a malformed body returns <code>400</code> before the use case runs. A controller does three jobs only: parse, call, translate.' },
        { path: ['uc', 'catalog', 'uc'], label: 'price_of("apple")', badgeAt: 'catalog', badge: '120¢', ms: 2,
          title: 'Prices come through a port',
          detail: 'The use case depends on the <code>PriceCatalog</code> interface, not a concrete store. An unknown SKU raises <code>InvalidOrder</code>, mapped to <code>422</code>.' },
        { path: ['uc', 'order', 'uc'], label: 'Order.place(lines, now)', badgeAt: 'order', badge: 'PENDING', ms: 0.1,
          title: 'The domain enforces the rules',
          detail: '1–50 lines, positive quantities. Pure Python: no SQL, no HTTP, and even the time is passed in rather than read from the clock.' },
        { path: ['uc', 'uow'], label: 'save PENDING; commit', ms: 5, title: 'Record intent first',
          detail: 'A database and a remote payment API can\'t share one atomic transaction. So the order is saved <b>before</b> the irreversible call.' },
        { path: ['uc', 'pay', 'uc'], label: 'charge(360¢, key=order.id)', badgeAt: 'pay', badge: 'charged', tone: 'ok', ms: 300,
          title: 'Charge with an idempotency key',
          detail: 'The order id doubles as the idempotency key, so retrying this use case can never charge twice.' },
        { path: ['uc', 'order'], label: 'mark_paid()', badgeAt: 'order', badge: 'PAID + OrderPaid', ms: 0.1,
          title: 'The domain records an event',
          detail: 'The order appends <code>OrderPaid</code> to its own event list. Publishing is I/O, and the domain does no I/O.' },
        { path: ['uc', 'uow'], label: 'save PAID; commit', ms: 5, title: 'Record the outcome',
          detail: 'The use case owns the transaction boundary; repositories never commit on their own.' },
        { path: ['uc', 'pub'], label: 'OrderPaid', tone: 'ok', ms: 2, title: 'Publish only after the commit',
          detail: 'If publishing must be guaranteed, write the event to an outbox table in the same transaction instead.' },
        { path: ['uc', 'http'], label: '201 {order_id}', tone: 'ok', ms: 2, title: 'Created',
          detail: 'Stored as <code>PAID</code> with a total of 360¢. Every dependency arrow in the code points toward the domain.' },
      ],
    },
    {
      id: 'declined', name: 'Card declined',
      summary: 'Ten apples (1,200¢) against a gateway that declines anything over 500¢. The order is <b>kept, as cancelled</b>, and nothing is published.',
      steps: [
        { path: ['http', 'uc'], label: 'PlaceOrderCommand', ms: 2, title: 'A bigger order arrives', detail: 'Same path through parsing, pricing and the domain rules.' },
        { path: ['uc', 'uow'], label: 'save PENDING; commit', ms: 5, title: 'Intent recorded',
          detail: 'Even an order that will fail exists in the database first, so there is a record of the attempt.' },
        { path: ['uc', 'pay', 'uc'], label: 'charge(1200¢)', badgeAt: 'pay', badge: 'PaymentDeclined', tone: 'err', ms: 300,
          title: 'The card is declined', detail: 'The gateway raises a domain error, <code>PaymentDeclined</code>, not a vendor-specific exception.' },
        { path: ['uc', 'order'], label: 'cancel()', badgeAt: 'order', badge: 'CANCELLED', tone: 'warn', ms: 0.1,
          title: 'The domain decides what "cancel" means', detail: 'A PAID order can\'t be cancelled, only refunded. That rule lives in <code>Order</code>, not in the use case.' },
        { path: ['uc', 'uow'], label: 'save CANCELLED; commit', ms: 5, title: 'The outcome is stored',
          detail: 'No events were recorded, so nothing goes to the publisher.' },
        { path: ['uc', 'http'], label: 'raise PaymentDeclined', badgeAt: 'http', badge: '402', tone: 'err', ms: 2,
          title: 'The adapter maps the error',
          detail: 'Domain errors become protocol codes only at the edge: <code>InvalidOrder</code> → 422, <code>PaymentDeclined</code> → 402.' },
      ],
    },
    {
      id: 'timeout', name: 'Charge timeout',
      summary: 'The charge call times out: <b>did it charge or not?</b> Because intent was saved first and the key is the order id, a retry is safe.',
      steps: [
        { path: ['uc', 'uow'], label: 'save PENDING; commit', ms: 5, title: 'Intent recorded',
          detail: 'The reverse order (charge, then save) would be unrecoverable if the save failed: charged, no order, and nobody knows.' },
        { path: ['uc', 'pay'], label: 'charge(key=order.id)', badgeAt: 'pay', badge: 'timeout?', tone: 'warn', ms: 5000,
          title: 'The outcome is unknown', detail: 'The provider may or may not have charged the card. Illustrative timeout.' },
        { at: 'uow', badge: 'order still PENDING', tone: 'warn', ms: 1, title: 'Nothing is lost',
          detail: 'A PENDING order exists. A retry, or a sweeper that looks for old PENDING orders, will finish the work.' },
        { path: ['uc', 'pay', 'uc'], label: 'retry, SAME key', badgeAt: 'pay', badge: 'duplicate → same result', tone: 'ok', ms: 300,
          title: 'Retry with the same key',
          detail: 'The provider sees the key again and returns the first result instead of charging a second time.' },
        { path: ['uc', 'order'], label: 'mark_paid()', ms: 0.1, title: 'Outcome applied in the domain', detail: 'The order moves from PENDING to PAID and records <code>OrderPaid</code>.' },
        { path: ['uc', 'uow'], label: 'save PAID; commit', tone: 'ok', ms: 5, title: 'Finished exactly once',
          detail: 'Record intent, make the external call idempotent, record the outcome, and make sure something finishes interrupted work.' },
      ],
    },
  ],
});
}
