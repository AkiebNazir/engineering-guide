package main

import "fmt"

/*
================================================================================
QUESTION · LeetCode 28 · Find the Index of the First Occurrence in a String  [Easy]
https://leetcode.com/problems/find-the-index-of-the-first-occurrence-in-a-string/
================================================================================

Given two strings `needle` and `haystack`, return the index of the first
occurrence of `needle` in `haystack`, or -1 if `needle` is not part of
`haystack`.

Example 1:
    Input:  haystack = "sadbutsad", needle = "sad"
    Output: 0
    Explanation: "sad" occurs at index 0 and index 6. The first occurrence
    is at index 0, so we return 0.

Example 2:
    Input:  haystack = "leetcode", needle = "leeto"
    Output: -1
    Explanation: "leeto" does not occur in "leetcode", so we return -1.

Constraints:
    1 <= haystack.length, needle.length <= 10^4
    haystack and needle consist of only lowercase English characters.

Follow-up: this is the classic "implement strStr()" problem -- the naive
O(nm) double loop passes on LeetCode's test data, but the real interview
signal is whether you know a linear-time string-matching algorithm
(Knuth-Morris-Pratt or Rabin-Karp) and can implement one from scratch.
================================================================================
*/

func main() {
	fmt.Println("Solution for Find the Index of the First Occurrence in a String not implemented yet")
}
