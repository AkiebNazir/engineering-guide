package main

import "fmt"

/*
================================================================================
QUESTION · LeetCode 9 · Palindrome Number                            [Easy]
https://leetcode.com/problems/palindrome-number/
================================================================================

Given an integer `x`, return True if `x` is a palindrome (reads the same
forwards and backwards), and False otherwise.

Example 1:
    Input:  x = 121
    Output: True

Example 2:
    Input:  x = -121
    Output: False
    Explanation: reads "121-" backwards, so it isn't a palindrome. The minus
    sign only appears on one side.

Example 3:
    Input:  x = 10
    Output: False
    Explanation: reads 01 backwards, and 01 is not the same as 10.

Constraints:
    -2^31 <= x <= 2^31 - 1

Follow-up: could you solve it without converting the integer to a string?
================================================================================
*/

func main() {
	fmt.Println("Solution for Palindrome Number not implemented yet")
}
