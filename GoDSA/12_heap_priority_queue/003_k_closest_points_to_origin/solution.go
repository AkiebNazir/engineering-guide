package main

import "fmt"

/*
================================================================================
QUESTION · LeetCode 973 · K Closest Points to Origin                  [Medium]
https://leetcode.com/problems/k-closest-points-to-origin/
================================================================================

Given an array of `points` where points[i] = [xi, yi] represents a point on
the X-Y plane and an integer `k`, return the k closest points to the origin
(0, 0).

The distance between two points on the X-Y plane is the Euclidean distance
(sqrt((x1 - x2)^2 + (y1 - y2)^2)).

You may return the answer in ANY order. The answer is guaranteed to be
unique (except for the order it is in).

Example 1:
    Input:  points = [[1,3],[-2,2]], k = 1
    Output: [[-2,2]]
    Explanation:
        distance((1,3), origin)  = sqrt(10)
        distance((-2,2), origin) = sqrt(8)
        (-2,2) is closer, so it is the answer.

Example 2:
    Input:  points = [[3,3],[5,-1],[-2,4]], k = 2
    Output: [[3,3],[-2,4]]
    Explanation: any order of these two points is acceptable.

Constraints:
    1 <= k <= points.length <= 10^4
    -10^4 <= xi, yi <= 10^4
*/

func main() {
	fmt.Println("Solution for K Closest Points to Origin not implemented yet")
}
